{
  "name": "omarchy-widget-template",
  "version": "0.1.0",
  "private": true,
  "description": "Omarchy bar widget template - dev harness for the Model.js data layer",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
